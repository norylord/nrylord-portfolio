import React from 'react';
import Layout from "../Layout/Layout";

const Projects = () => {
    return (
        <Layout>
            <h1>Projects</h1>
        </Layout>
    );
};

export default Projects;